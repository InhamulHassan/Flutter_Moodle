import 'package:flutter/material.dart';
import 'package:moodle_app/Pages/Home/screen.dart';

final timetableScreen = Screen(
  title: 'Timetable',
  buildContext: (BuildContext context) {
    return Container(
      child: Center(
        child: Text('Timetable Screen'),
      ),
    );
  },
);
